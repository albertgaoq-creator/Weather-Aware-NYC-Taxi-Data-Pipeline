name: weather_aware_taxi
version: 1.0.0
config-version: 2

profile: weather_aware_taxi

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  weather_aware_taxi:
    +materialized: view
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: mart
      +materialized: table
